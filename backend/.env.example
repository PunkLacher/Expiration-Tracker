PORT=4000
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:4000
MAGIC_LINK_BASE_URL=http://localhost:4000
CORS_ORIGINS=http://localhost:5173
JWT_SECRET=change-this-in-production
SESSION_DAYS=60
MAGIC_LINK_MINUTES=10
COOKIE_SAME_SITE=lax
COOKIE_SECURE=false
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/expiration_tracker
DATABASE_SSL=false

# Preferred email mode for Render free tier: Brevo API
BREVO_API_KEY=your_brevo_api_key
EMAIL_FROM=noreply@pooleng.com
EMAIL_FROM_NAME=Pool Engineering

# Optional SMTP fallback mode (not recommended on Render free due outbound port limits)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
